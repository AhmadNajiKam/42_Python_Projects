{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"42 Python Projects","text":"<p>Source \u2192 github.com/AhmadNajiKam/42_Python_Projects</p> <p>A living reference for every Python project completed at 42 School \u2014 covering functional programming, data validation, file I/O, typing, and more.</p>"},{"location":"#whats-in-here","title":"What's in here?","text":"Section What you'll find Tutorials Step-by-step walkthroughs that build real concepts from scratch How-To Guides Focused recipes for specific tasks (submitting exercises, debugging, testing) Reference Authoritative descriptions of every project, concept, and rule Projects Deep-dives into individual project modules"},{"location":"#project-map","title":"Project Map","text":"<pre><code>graph TD\n    A[42 Python Projects] --&gt; B[Functional Programming]\n    A --&gt; C[Data Validation]\n    A --&gt; D[File &amp; Stream I/O]\n    A --&gt; E[Typing &amp; Protocols]\n    A --&gt; F[OOP &amp; Decorators]\n\n    B --&gt; B1[FuncMage]\n    C --&gt; C1[CosmicData]\n    C --&gt; C2[GardenGuardian]\n    D --&gt; D1[DataArchivist]\n    D --&gt; D2[DataDeck]\n    E --&gt; E1[TheCodex]\n    E --&gt; E2[TheMatrix]\n    F --&gt; F1[CodeCultivation]\n    F --&gt; F2[GrowingCode]</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Clone the repo\ngit clone https://github.com/AhmadNajiKam/42_Python_Projects.git\ncd 42_Python_Projects\n\n# Enter any project\ncd FuncMage\n\n# Install dependencies (if any)\npip install -r requirements.txt\n\n# Run the main script\npython main.py\n</code></pre>"},{"location":"#skills-covered","title":"Skills Covered","text":"<ul> <li>Lambdas &amp; Higher-Order Functions \u2014 <code>map</code>, <code>filter</code>, <code>functools.reduce</code></li> <li>Closures &amp; Decorators \u2014 wrapping callables, <code>functools.wraps</code>, retry/timer patterns</li> <li>Pydantic v2 \u2014 <code>BaseModel</code>, field validators, nested models, environment config</li> <li>Generators &amp; Iterators \u2014 <code>yield</code>, lazy evaluation, generator expressions</li> <li>Python Typing \u2014 <code>Protocol</code>, <code>TypeVar</code>, generics, <code>Annotated</code></li> <li>File Streams \u2014 context managers, binary vs text mode, stream buffering</li> <li><code>python-dotenv</code> \u2014 <code>.env</code> loading, <code>BaseSettings</code></li> </ul> <p>New to 42?</p> <p>Start with the First Decorator Tutorial \u2014 it introduces closures, <code>functools.wraps</code>, and the <code>@</code> syntax in a single guided session.</p>"},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/","title":"ADR 0001: Use Nested Function for Isolated Decorator Validation","text":""},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#context","title":"Context","text":"<p>In the <code>MageGuild</code> class, the <code>cast_spell</code> method needs to apply runtime parameter validation using the <code>@power_validator</code> decorator.</p> <p>Python decorators are typically applied at the function definition level. If we apply the decorator directly to the public <code>cast_spell</code> method, the signature expected by the decorator might clash with the public instance method signature (which automatically passes <code>self</code> as the first argument).</p> <p>Furthermore, we only want the validation logic to apply strictly to the core execution of casting the spell, keeping it cleanly isolated from any potential preprocessing or postprocessing logic inside <code>cast_spell</code>.</p>"},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#decision","title":"Decision","text":"<p>We decided to encapsulate the core spell execution logic inside a nested, inner function named <code>_cast</code> directly within the <code>cast_spell</code> method, and apply the <code>@power_validator</code> decorator to this inner function.</p> <pre><code>def cast_spell(self, spell_name: str, power: int) -&gt; str:\n    @power_validator(min_power=10)\n    def _cast(power: int, spell_name: str) -&gt; str:\n        return f\"Successfully cast {spell_name} with {power} power\"\n    return str(_cast(power, spell_name))\n</code></pre> <p>The leading underscore in <code>_cast</code> explicitly marks it as a private helper implementation detail that is completely hidden from the rest of the class.</p>"},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#consequences","title":"Consequences","text":""},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#positive","title":"Positive","text":"<ul> <li>Signature Alignment: Avoids passing <code>self</code> into the <code>@power_validator</code> logic, ensuring the decorator only interacts with the raw validation parameters (<code>power</code>).</li> <li>Encapsulation: The validation routine is localized entirely within <code>cast_spell</code>. No other methods in <code>MageGuild</code> can accidentally call or interfere with <code>_cast</code>.</li> <li>Readability: Keeping the execution close to the validation logic makes it immediately clear how the parameters are being evaluated.</li> </ul>"},{"location":"adr/0001-use-nested-function-for-isolated-decorator-validation/#negative","title":"Negative","text":"<ul> <li>Performance Overhead: The <code>_cast</code> function is redefined dynamically every single time <code>cast_spell</code> is called. For this codebase's scale, the overhead of creating a function object is negligible, but it is worth noting for tight loops.</li> <li>Testing: The inner <code>_cast</code> function cannot be unit-tested in isolation; it can only be tested implicitly by calling <code>cast_spell</code>.</li> </ul>"},{"location":"how-to/","title":"How-To Guides","text":"<p>How-to guides are task-oriented \u2014 they answer \"how do I do X?\" and assume you already know why you want to do it. Each guide is a focused recipe with minimal explanation.</p>"},{"location":"how-to/#available-guides","title":"Available Guides","text":"<ul> <li> <p> Submit a 42 Exercise</p> <p>The exact steps to normalise, lint, and push an exercise for peer review.</p> </li> <li> <p> Run Tests Locally</p> <p>Run <code>pytest</code> against an exercise before submitting, including fixture setup and common flags.</p> </li> <li> <p> Debug with Python's Traceback</p> <p>Read a Python traceback top-to-bottom, isolate the real error line, and use <code>pdb</code> to inspect state.</p> </li> </ul>"},{"location":"how-to/debug-traceback/","title":"How to Debug with Python's Traceback","text":"<p>Read a Python traceback confidently and reach the root cause in under a minute.</p>"},{"location":"how-to/debug-traceback/#anatomy-of-a-traceback","title":"Anatomy of a Traceback","text":"<pre><code>Traceback (most recent call last):        # (1) header \u2014 always present\n  File \"main.py\", line 14, in &lt;module&gt;   # (2) outermost call\n    result = cast_spell(\"fireball\")\n  File \"spells.py\", line 7, in cast_spell # (3) intermediate frames\n    return SPELLS[name]()\nKeyError: 'fireball'                      # (4) THE actual error \u2014 read this first\n</code></pre> <p>Rule: read from the bottom up.</p> <ol> <li>The last line is the error type and message \u2014 this is your real clue.</li> <li>The frame directly above it is where the error occurred.</li> <li>Higher frames show how you got there.</li> </ol>"},{"location":"how-to/debug-traceback/#step-by-step-isolate-the-error","title":"Step-by-Step: Isolate the Error","text":""},{"location":"how-to/debug-traceback/#step-1-read-the-error-type","title":"Step 1 \u2014 Read the error type","text":"Error type Likely cause <code>TypeError</code> Wrong argument type or count <code>AttributeError</code> Called <code>.something</code> on <code>None</code> or wrong type <code>KeyError</code> Dict key doesn't exist <code>ValidationError</code> Pydantic model received invalid input <code>ImportError</code> Module not found or circular import <code>IndentationError</code> Tabs mixed with spaces, or wrong indentation level"},{"location":"how-to/debug-traceback/#step-2-go-to-the-file-and-line","title":"Step 2 \u2014 Go to the file and line","text":"<p>Open the file and line number from the bottom frame. Read the actual line, not just the error message.</p>"},{"location":"how-to/debug-traceback/#step-3-print-intermediate-values","title":"Step 3 \u2014 Print intermediate values","text":"<pre><code>def cast_spell(name: str):\n    print(f\"DEBUG: SPELLS keys = {list(SPELLS.keys())}\")  # \u2190 add this\n    print(f\"DEBUG: name = {repr(name)}\")                  # \u2190 and this\n    return SPELLS[name]()\n</code></pre> <p><code>repr()</code> reveals hidden characters: <code>repr(\"fireball \")</code> \u2192 <code>'fireball '</code> \u2014 there's a trailing space!</p>"},{"location":"how-to/debug-traceback/#step-4-use-pdb-for-complex-state","title":"Step 4 \u2014 Use <code>pdb</code> for complex state","text":"<pre><code>python -m pdb main.py\n</code></pre> <p>Or insert a breakpoint directly:</p> <pre><code>def cast_spell(name: str):\n    breakpoint()          # drops you into pdb at this line (Python 3.7+)\n    return SPELLS[name]()\n</code></pre> <p>Useful <code>pdb</code> commands:</p> Command Action <code>n</code> Execute next line <code>s</code> Step into a function call <code>p expr</code> Print the value of <code>expr</code> <code>l</code> List source around current line <code>q</code> Quit"},{"location":"how-to/debug-traceback/#pydantic-validationerror-specifically","title":"Pydantic <code>ValidationError</code> Specifically","text":"<p>Pydantic errors are structured \u2014 don't just print the exception:</p> <pre><code>from pydantic import ValidationError\nfrom your_module import ContactLog\n\ntry:\n    log = ContactLog(signal_id=\"BAD\", frequency_mhz=-1)\nexcept ValidationError as e:\n    print(e.error_count(), \"errors\")\n    for error in e.errors():\n        print(f\"  Field: {error['loc']}\")\n        print(f\"  Message: {error['msg']}\")\n        print(f\"  Input: {error['input']}\")\n</code></pre> <p>Output: <pre><code>2 errors\n  Field: ('signal_id',)\n  Message: Value error, signal_id must start with 'SIG-'\n  Input: BAD\n  Field: ('frequency_mhz',)\n  Message: Value error, frequency_mhz must be positive\n  Input: -1\n</code></pre></p>"},{"location":"how-to/debug-traceback/#quick-checklist","title":"Quick Checklist","text":"<ul> <li>[ ] Read the last line of the traceback first</li> <li>[ ] Identify the file and line in the bottom frame</li> <li>[ ] Add <code>print(repr(variable))</code> to check exact values</li> <li>[ ] Use <code>breakpoint()</code> if the state is complex</li> <li>[ ] For Pydantic errors, iterate <code>e.errors()</code> \u2014 not just <code>print(e)</code></li> </ul>"},{"location":"how-to/run-tests/","title":"How to Run Tests Locally","text":"<p>Run <code>pytest</code> against an exercise before submitting to catch failures early.</p>"},{"location":"how-to/run-tests/#install-pytest","title":"Install pytest","text":"<pre><code>pip install pytest pytest-cov\n</code></pre>"},{"location":"how-to/run-tests/#basic-run","title":"Basic run","text":"<p>From the project root:</p> <pre><code>pytest                          # discover and run all tests\npytest ex00/                    # run tests only in ex00/\npytest -v                       # verbose: show each test name\npytest -x                       # stop at first failure\npytest -k \"timer\"               # run only tests whose name contains \"timer\"\n</code></pre>"},{"location":"how-to/run-tests/#running-against-a-specific-model-with-pydantic","title":"Running against a specific model with Pydantic","text":"<p>Some exercises validate that your models raise <code>ValidationError</code> correctly. Use <code>pytest.raises</code>:</p> <pre><code># test_models.py\nimport pytest\nfrom pydantic import ValidationError\nfrom your_module import ContactLog\n\ndef test_invalid_signal_id_raises():\n    with pytest.raises(ValidationError):\n        ContactLog(signal_id=\"NOPE\", frequency_mhz=1420.4)\n\ndef test_negative_frequency_raises():\n    with pytest.raises(ValidationError):\n        ContactLog(signal_id=\"SIG-001\", frequency_mhz=-1.0)\n</code></pre> <pre><code>pytest test_models.py -v\n</code></pre>"},{"location":"how-to/run-tests/#coverage-report","title":"Coverage report","text":"<pre><code>pytest --cov=. --cov-report=term-missing\n</code></pre> <p>Aim for \u2265 80% coverage before submitting. The <code>term-missing</code> flag shows exactly which lines aren't covered.</p>"},{"location":"how-to/run-tests/#common-failure-patterns","title":"Common failure patterns","text":"Symptom Likely cause <code>ModuleNotFoundError</code> Wrong working directory; run from the project root <code>ImportError: cannot import name X</code> Typo in your class or function name <code>ValidationError</code> on valid input Field validator is too strict; check boundary conditions All tests pass locally, fail at 42 Python version mismatch; check <code>python --version</code> matches the subject"},{"location":"how-to/run-tests/#using-a-env-file-in-tests","title":"Using a <code>.env</code> file in tests","text":"<p>If your exercise uses <code>pydantic-settings</code> / <code>python-dotenv</code>:</p> <pre><code># Create a test-specific .env\ncp .env .env.test\n# Edit .env.test with safe test values\n\n# Run with the test env\nENV_FILE=.env.test pytest\n</code></pre> <p>Or override in <code>conftest.py</code>:</p> <pre><code># conftest.py\nimport os\nimport pytest\n\n@pytest.fixture(autouse=True)\ndef set_test_env(monkeypatch):\n    monkeypatch.setenv(\"DATABASE_URL\", \"sqlite:///:memory:\")\n    monkeypatch.setenv(\"DEBUG\", \"true\")\n</code></pre>"},{"location":"how-to/submit-exercise/","title":"How to Submit a 42 Exercise","text":"<p>This guide covers the exact steps from finishing your code to a clean push ready for peer review.</p>"},{"location":"how-to/submit-exercise/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python \u2265 3.10 installed</li> <li><code>git</code> configured with your 42 intra credentials</li> <li>The exercise repo cloned locally</li> </ul>"},{"location":"how-to/submit-exercise/#steps","title":"Steps","text":""},{"location":"how-to/submit-exercise/#1-check-the-exercise-spec","title":"1. Check the exercise spec","text":"<p>Re-read the subject PDF one final time. Pay attention to:</p> <ul> <li>Exact filenames \u2014 42 graders are case-sensitive.</li> <li>Forbidden imports \u2014 some exercises ban <code>import os</code>, <code>sys</code>, etc.</li> <li>Expected output format \u2014 trailing newlines, spacing, and encoding matter.</li> </ul>"},{"location":"how-to/submit-exercise/#2-lint-with-pycodestyle","title":"2. Lint with <code>pycodestyle</code>","text":"<p>42 Python exercises are graded against PEP 8. Run:</p> <pre><code>pycodestyle --max-line-length=79 your_file.py\n</code></pre> <p>Fix every reported issue before continuing. Common fixes:</p> Error Fix <code>E302</code> Add two blank lines before top-level functions/classes <code>E501</code> Break long lines; use implicit line continuation inside <code>()</code> <code>W291</code> Remove trailing whitespace <code>E711</code> Replace <code>== None</code> with <code>is None</code>"},{"location":"how-to/submit-exercise/#3-verify-with-the-provided-test-script","title":"3. Verify with the provided test script","text":"<p>If the subject includes a <code>test.py</code> or <code>main.py</code>:</p> <pre><code>python test.py\n</code></pre> <p>All assertions must pass silently. Any output means a failure.</p>"},{"location":"how-to/submit-exercise/#4-remove-__pycache__-and-pyc-files","title":"4. Remove <code>__pycache__</code> and <code>.pyc</code> files","text":"<pre><code>find . -type d -name __pycache__ -exec rm -rf {} + 2&gt;/dev/null\nfind . -name \"*.pyc\" -delete\n</code></pre> <p>Or add to your <code>.gitignore</code> (already done in this repo):</p> <pre><code>__pycache__/\n*.pyc\n*.pyo\n.env\n</code></pre>"},{"location":"how-to/submit-exercise/#5-stage-only-the-required-files","title":"5. Stage only the required files","text":"<pre><code>git add ex00/your_file.py   # add only what the subject asks for\ngit status                  # confirm nothing extra is staged\n</code></pre> <p>Never commit <code>.env</code> files</p> <p>If your exercise uses <code>python-dotenv</code>, make sure <code>.env</code> is in <code>.gitignore</code>  before staging. Committing secrets is an automatic fail at 42.</p>"},{"location":"how-to/submit-exercise/#6-commit-with-a-conventional-message","title":"6. Commit with a conventional message","text":"<pre><code>git commit -m \"feat(FuncMage): implement spell_timer decorator\"\n</code></pre> <p>Format: <code>&lt;type&gt;(&lt;scope&gt;): &lt;short description&gt;</code> Common types: <code>feat</code>, <code>fix</code>, <code>refactor</code>, <code>docs</code>, <code>test</code></p>"},{"location":"how-to/submit-exercise/#7-push","title":"7. Push","text":"<pre><code>git push origin main\n</code></pre>"},{"location":"how-to/submit-exercise/#8-verify-on-github","title":"8. Verify on GitHub","text":"<p>Open the repo on GitHub and confirm:</p> <ul> <li>The correct files are present in the correct directories.</li> <li>No secrets, <code>__pycache__</code>, or unrelated files were pushed.</li> <li>The commit message is clean.</li> </ul>"},{"location":"how-to/submit-exercise/#checklist","title":"Checklist","text":"<ul> <li>[ ] Re-read the subject PDF</li> <li>[ ] <code>pycodestyle</code> reports zero errors</li> <li>[ ] Test script passes</li> <li>[ ] <code>__pycache__</code> removed</li> <li>[ ] <code>.env</code> not staged</li> <li>[ ] Only required files staged</li> <li>[ ] Conventional commit message written</li> <li>[ ] Pushed and verified on GitHub</li> </ul>"},{"location":"projects/","title":"Projects","text":"<p>Deep-dives into individual project modules \u2014 design decisions, implementation notes, and lessons learned.</p> <ul> <li> <p> FuncMage</p> <p>Decorators, HOF, closures. <code>spell_timer</code>, <code>power_validator</code>, <code>retry_spell</code>, <code>MageGuild</code>.</p> </li> <li> <p> CosmicData</p> <p>Pydantic v2 models for alien contact logs and space crew management.</p> </li> <li> <p> DataArchivist</p> <p>File stream management, context managers, binary and text mode I/O.</p> </li> <li> <p> TheMatrix</p> <p>Python Protocols, structural subtyping, <code>TypeVar</code>, and generic callables.</p> </li> </ul>"},{"location":"projects/cosmicdata/","title":"CosmicData","text":"<p>Folder: <code>CosmicData/</code> Theme: Pydantic v2 data validation \u2014 alien contact logs, space crew management Key files: <code>models.py</code>, <code>settings.py</code></p>"},{"location":"projects/cosmicdata/#model-hierarchy","title":"Model Hierarchy","text":"<pre><code>SpaceStation\n\u251c\u2500\u2500 station_id: str\n\u251c\u2500\u2500 operational: bool\n\u2514\u2500\u2500 crew: list[AlienCrewMember]\n        \u251c\u2500\u2500 name: str\n        \u251c\u2500\u2500 role: str\n        \u2514\u2500\u2500 health: int (0\u2013100)\n\nContactLog\n\u251c\u2500\u2500 signal_id: str  (must start with \"SIG-\")\n\u251c\u2500\u2500 received_at: datetime\n\u251c\u2500\u2500 frequency_mhz: float (&gt; 0)\n\u251c\u2500\u2500 origin: Location\n\u2502       \u251c\u2500\u2500 constellation: str\n\u2502       \u2514\u2500\u2500 light_years_away: float\n\u2514\u2500\u2500 decoded_message: str | None\n</code></pre>"},{"location":"projects/cosmicdata/#key-design-decisions","title":"Key Design Decisions","text":""},{"location":"projects/cosmicdata/#signal_id-normalisation","title":"<code>signal_id</code> normalisation","text":"<p>The validator both validates and transforms \u2014 it uppercases the ID so <code>\"sig-001\"</code> and <code>\"SIG-001\"</code> both result in <code>\"SIG-001\"</code>. This prevents duplicate entries from differing only in case.</p> <pre><code>@field_validator(\"signal_id\")\n@classmethod\ndef signal_id_format(cls, v: str) -&gt; str:\n    v = v.upper()\n    if not v.startswith(\"SIG-\"):\n        raise ValueError(\"signal_id must start with 'SIG-'\")\n    return v\n</code></pre>"},{"location":"projects/cosmicdata/#health-range-validation","title":"<code>health</code> range validation","text":"<p>Using <code>Annotated</code> + <code>Field</code> is cleaner than a validator for simple numeric constraints:</p> <pre><code>from typing import Annotated\nfrom pydantic import Field\n\nhealth: Annotated[int, Field(ge=0, le=100)] = 100\n</code></pre>"},{"location":"projects/cosmicdata/#environment-config","title":"Environment config","text":"<p>Sensitive values (DB URLs, API keys) are loaded via <code>pydantic-settings</code>:</p> <pre><code>from pydantic_settings import BaseSettings\n\nclass CosmicSettings(BaseSettings):\n    database_url: str\n    signal_batch_size: int = 100\n    model_config = {\"env_file\": \".env\"}\n</code></pre>"},{"location":"projects/cosmicdata/#lessons-learned","title":"Lessons Learned","text":"<ul> <li><code>model_dump_json()</code> \u2014 prefer over <code>json.dumps(model.model_dump())</code> because it handles <code>datetime</code> serialisation automatically.</li> <li>Nested dict \u2192 nested model \u2014 Pydantic v2 automatically coerces a raw dict into a nested <code>BaseModel</code> at instantiation. You don't need to instantiate <code>Location(...)</code> manually.</li> <li><code>pydantic-settings</code> is a separate package \u2014 <code>pip install pydantic-settings</code> \u2014 not bundled with <code>pydantic</code> in v2.</li> </ul>"},{"location":"projects/dataarchvist/","title":"DataArchivist","text":"<p>Folder: <code>DataArchivist/</code> Theme: File stream management \u2014 context managers, binary vs text mode, stream buffering Key file: <code>archivist.py</code></p>"},{"location":"projects/dataarchvist/#core-patterns","title":"Core Patterns","text":""},{"location":"projects/dataarchvist/#reading-files-safely","title":"Reading files safely","text":"<pre><code>with open(\"archive.txt\", \"r\", encoding=\"utf-8\") as f:\n    content = f.read()\n# File is automatically closed even if an exception is raised\n</code></pre> <p>Always specify <code>encoding=\"utf-8\"</code> explicitly \u2014 the default encoding is platform-dependent (Windows uses <code>cp1252</code> by default).</p>"},{"location":"projects/dataarchvist/#binary-mode","title":"Binary mode","text":"<p>For non-text files (images, compressed archives, pickled data):</p> <pre><code>with open(\"data.bin\", \"rb\") as f:\n    header = f.read(4)   # read exactly 4 bytes\n    rest = f.read()\n\nwith open(\"output.bin\", \"wb\") as f:\n    f.write(b\"\\x89PNG\\r\\n\")\n</code></pre>"},{"location":"projects/dataarchvist/#line-by-line-iteration-memory-efficient","title":"Line-by-line iteration (memory-efficient)","text":"<p>For large files, don't load everything into memory:</p> <pre><code>with open(\"large.log\", \"r\", encoding=\"utf-8\") as f:\n    for line in f:                # file object is an iterator\n        process(line.rstrip(\"\\n\"))\n</code></pre>"},{"location":"projects/dataarchvist/#writing-and-appending","title":"Writing and appending","text":"<pre><code># Overwrite\nwith open(\"output.txt\", \"w\", encoding=\"utf-8\") as f:\n    f.write(\"fresh content\\n\")\n\n# Append\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as f:\n    f.write(\"new entry\\n\")\n</code></pre>"},{"location":"projects/dataarchvist/#stream-positions","title":"Stream Positions","text":"<pre><code>with open(\"data.txt\", \"r+\", encoding=\"utf-8\") as f:\n    f.seek(0)           # move to start\n    content = f.read()\n    f.seek(0, 2)        # move to end (whence=2)\n    pos = f.tell()      # current byte position\n</code></pre>"},{"location":"projects/dataarchvist/#lessons-learned","title":"Lessons Learned","text":"<ul> <li>Always use context managers (<code>with open(...)</code>) \u2014 <code>f.close()</code> in a <code>finally</code> block is verbose and easy to forget.</li> <li>Binary vs text mode matters \u2014 opening a <code>.png</code> in text mode on Windows silently corrupts <code>\\r\\n</code> sequences.</li> <li><code>encoding=\"utf-8\"</code> is not optional \u2014 hardcode it; never rely on the system default.</li> <li>Large files = iterator \u2014 <code>f.readlines()</code> loads the whole file into RAM; <code>for line in f</code> streams it lazily.</li> </ul>"},{"location":"projects/funcmage/","title":"FuncMage","text":"<p>Folder: <code>FuncMage/</code> Theme: Functional programming \u2014 lambdas, higher-order functions, closures, decorators Key file: <code>decorator_mastery.py</code></p>"},{"location":"projects/funcmage/#overview","title":"Overview","text":"<p>FuncMage is a decorator-focused exercise framed around spellcasting. Each function models a real decorator pattern:</p> Decorator Pattern <code>spell_timer</code> Execution timing wrapper <code>power_validator</code> Input validation guard <code>retry_spell</code> Retry with exponential backoff <code>MageGuild</code> Class-based decorator with state"},{"location":"projects/funcmage/#spell_timer","title":"<code>spell_timer</code>","text":"<p>Wraps any callable and prints its execution time.</p> <pre><code>import functools\nimport time\n\ndef spell_timer(func):\n    \"\"\"Measure and print the wall-clock time of a function call.\"\"\"\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        elapsed = time.perf_counter() - start\n        print(f\"\u23f1  {func.__name__} executed in {elapsed:.6f}s\")\n        return result\n    return wrapper\n</code></pre> <p>Design notes:</p> <ul> <li>Uses <code>time.perf_counter()</code> (not <code>time.time()</code>) for sub-millisecond precision.</li> <li><code>functools.wraps</code> preserves <code>__name__</code> and <code>__doc__</code> \u2014 required by the exercise spec.</li> <li>Returns <code>result</code> so the decorator is transparent to callers.</li> </ul>"},{"location":"projects/funcmage/#power_validator","title":"<code>power_validator</code>","text":"<p>Guards a function against out-of-range input.</p> <pre><code>def power_validator(min_power: int, max_power: int):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(power: int, *args, **kwargs):\n            if not (min_power &lt;= power &lt;= max_power):\n                raise ValueError(\n                    f\"Power {power} out of range [{min_power}, {max_power}]\"\n                )\n            return func(power, *args, **kwargs)\n        return wrapper\n    return decorator\n\n@power_validator(min_power=1, max_power=100)\ndef cast_spell(power: int) -&gt; str:\n    return f\"Spell at power {power}!\"\n</code></pre> <p>Design notes: This is a parametrised decorator \u2014 a factory that returns a decorator. The three-level nesting (<code>power_validator</code> \u2192 <code>decorator</code> \u2192 <code>wrapper</code>) is the standard pattern.</p>"},{"location":"projects/funcmage/#retry_spell","title":"<code>retry_spell</code>","text":"<p>Retries a failing function up to <code>n</code> times with optional delay.</p> <pre><code>import time\nimport functools\n\ndef retry_spell(max_attempts: int = 3, delay: float = 0.0):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            last_exc = None\n            for attempt in range(1, max_attempts + 1):\n                try:\n                    return func(*args, **kwargs)\n                except Exception as exc:\n                    last_exc = exc\n                    if attempt &lt; max_attempts:\n                        time.sleep(delay)\n            raise last_exc\n        return wrapper\n    return decorator\n</code></pre> <p>Design notes: Re-raises the last exception after all attempts are exhausted so the caller gets a meaningful error rather than <code>None</code>.</p>"},{"location":"projects/funcmage/#mageguild","title":"<code>MageGuild</code>","text":"<p>A class-based decorator that tracks how many times each spell has been cast.</p> <pre><code>class MageGuild:\n    def __init__(self, func):\n        functools.update_wrapper(self, func)\n        self.func = func\n        self.cast_count = 0\n\n    def __call__(self, *args, **kwargs):\n        self.cast_count += 1\n        print(f\"\ud83c\udff0 Guild record: {self.func.__name__} cast {self.cast_count}x\")\n        return self.func(*args, **kwargs)\n\n@MageGuild\ndef fireball():\n    return \"\ud83d\udd25\"\n\nfireball()   # Guild record: fireball cast 1x\nfireball()   # Guild record: fireball cast 2x\nprint(fireball.cast_count)  # 2\n</code></pre> <p>Design notes: <code>functools.update_wrapper(self, func)</code> on a class instance is the equivalent of <code>@functools.wraps</code> for function decorators. It copies <code>__name__</code>, <code>__doc__</code>, <code>__module__</code>, and <code>__wrapped__</code> onto <code>self</code>.</p>"},{"location":"projects/funcmage/#lessons-learned","title":"Lessons Learned","text":"<ul> <li><code>functools.wraps</code> is non-negotiable. Every decorator in a real codebase must preserve metadata \u2014 without it, <code>help()</code>, logging, and test introspection break.</li> <li>Parametrised decorators have three levels of nesting \u2014 factory, decorator, wrapper. It's easy to forget to <code>return wrapper</code> inside <code>decorator</code>.</li> <li>Class-based decorators are useful when you need to track state between calls. The trade-off is more boilerplate.</li> <li><code>time.perf_counter()</code> is the right clock for benchmarking. <code>time.time()</code> can go backwards on NTP adjustments; <code>perf_counter</code> is monotonic.</li> </ul>"},{"location":"projects/thematrix/","title":"TheMatrix","text":"<p>Folder: <code>TheMatrix/</code> Theme: Python typing \u2014 <code>Protocol</code>, structural subtyping, <code>TypeVar</code>, <code>Callable</code> annotations Key file: <code>matrix.py</code></p>"},{"location":"projects/thematrix/#core-concepts","title":"Core Concepts","text":""},{"location":"projects/thematrix/#protocol-structural-subtyping","title":"<code>Protocol</code> \u2014 structural subtyping","text":"<p>Protocols define interfaces without inheritance. Any class that implements the required methods satisfies the protocol \u2014 no <code>class Foo(MyProtocol)</code> needed.</p> <pre><code>from typing import Protocol\n\nclass Transformable(Protocol):\n    def transform(self, value: int) -&gt; int: ...\n\nclass Doubler:                   # does NOT inherit from Transformable\n    def transform(self, value: int) -&gt; int:\n        return value * 2\n\ndef apply(t: Transformable, x: int) -&gt; int:\n    return t.transform(x)\n\napply(Doubler(), 5)  # \u2705 type-checks \u2014 Doubler satisfies Transformable structurally\n</code></pre>"},{"location":"projects/thematrix/#typevar-generic-functions","title":"<code>TypeVar</code> \u2014 generic functions","text":"<pre><code>from typing import TypeVar\n\nT = TypeVar(\"T\")\n\ndef identity(value: T) -&gt; T:\n    return value\n\nidentity(42)       # \u2192 int\nidentity(\"hello\")  # \u2192 str\n</code></pre>"},{"location":"projects/thematrix/#callable-annotations","title":"<code>Callable</code> annotations","text":"<pre><code>from typing import Callable\n\ndef apply_twice(f: Callable[[int], int], x: int) -&gt; int:\n    return f(f(x))\n\napply_twice(lambda x: x + 1, 0)  # \u2192 2\n</code></pre>"},{"location":"projects/thematrix/#why-protocols-over-abcs","title":"Why Protocols over ABCs?","text":"<code>Protocol</code> <code>ABC</code> Requires explicit inheritance No \u2705 Yes Works with third-party classes Yes \u2705 No Runtime <code>isinstance</code> check With <code>@runtime_checkable</code> Yes Best for Duck-typed interfaces Enforced contracts"},{"location":"projects/thematrix/#lessons-learned","title":"Lessons Learned","text":"<ul> <li>Protocols enable open systems \u2014 you can define an interface that third-party code satisfies without modifying it.</li> <li><code>@runtime_checkable</code> is needed for <code>isinstance(obj, MyProtocol)</code> at runtime, but adds a small overhead \u2014 prefer static type checking with <code>mypy</code>.</li> <li><code>TypeVar</code> needs a consistent name \u2014 by convention, <code>T</code>, <code>K</code>, <code>V</code> are common; always name the variable the same as the string: <code>T = TypeVar(\"T\")</code> not <code>T = TypeVar(\"Item\")</code>.</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>Reference material is information-oriented \u2014 accurate, complete, and structured for lookup rather than learning. Use this when you already know what you're looking for.</p>"},{"location":"reference/#contents","title":"Contents","text":"<ul> <li>Project Catalogue \u2014 every project module with its folder, skills covered, and submission files</li> <li>Python Concepts Cheatsheet \u2014 all HOF, decorator, Pydantic, generator, and typing patterns used across the curriculum</li> <li>42 Style Rules \u2014 PEP 8 rules enforced by 42's graders, with examples of what passes and what fails</li> </ul>"},{"location":"reference/concepts/","title":"Python Concepts Cheatsheet","text":"<p>Quick-reference for all patterns used across the 42 Python curriculum.</p>"},{"location":"reference/concepts/#higher-order-functions","title":"Higher-Order Functions","text":"<pre><code># map \u2014 transform every element\nlist(map(lambda x: x ** 2, [1, 2, 3]))        # [1, 4, 9]\n\n# filter \u2014 keep elements matching predicate\nlist(filter(lambda x: x % 2 == 0, [1,2,3,4])) # [2, 4]\n\n# reduce \u2014 fold left\nfrom functools import reduce\nreduce(lambda acc, x: acc + x, [1,2,3,4])      # 10\n\n# sorted with key\nsorted([\"banana\", \"fig\", \"apple\"], key=len)    # ['fig', 'apple', 'banana']\n</code></pre>"},{"location":"reference/concepts/#decorators","title":"Decorators","text":"<pre><code>import functools\n\ndef my_decorator(func):\n    @functools.wraps(func)          # preserves __name__, __doc__\n    def wrapper(*args, **kwargs):\n        # before\n        result = func(*args, **kwargs)\n        # after\n        return result\n    return wrapper\n\n@my_decorator\ndef my_function(): ...\n</code></pre> <p>Decorator with arguments (factory pattern):</p> <pre><code>def repeat(n):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            for _ in range(n):\n                func(*args, **kwargs)\n        return wrapper\n    return decorator\n\n@repeat(3)\ndef hello(): print(\"hi\")\n</code></pre>"},{"location":"reference/concepts/#generators","title":"Generators","text":"<pre><code># Generator function\ndef count_up(start, stop):\n    while start &lt;= stop:\n        yield start\n        start += 1\n\n# Generator expression (lazy)\nsquares = (x ** 2 for x in range(10))\n\n# Consume\nnext(squares)     # 0\nlist(squares)     # [1, 4, 9, ... 81]  (0 already consumed)\n\n# yield from \u2014 delegate to sub-generator\ndef chain(*iterables):\n    for it in iterables:\n        yield from it\n</code></pre>"},{"location":"reference/concepts/#pydantic-v2","title":"Pydantic v2","text":"<pre><code>from pydantic import BaseModel, field_validator\nfrom typing import Annotated\nfrom pydantic import Field\n\nclass Item(BaseModel):\n    name: str\n    price: Annotated[float, Field(gt=0)]          # greater-than constraint\n    tags: list[str] = []\n\n    @field_validator(\"name\")\n    @classmethod\n    def name_not_empty(cls, v: str) -&gt; str:\n        if not v.strip():\n            raise ValueError(\"name cannot be blank\")\n        return v.strip()\n\nitem = Item(name=\" Widget \", price=9.99)\nprint(item.name)              # \"Widget\"\nprint(item.model_dump())      # {'name': 'Widget', 'price': 9.99, 'tags': []}\nprint(item.model_dump_json()) # '{\"name\":\"Widget\",\"price\":9.99,\"tags\":[]}'\n</code></pre> <p>Settings from <code>.env</code>:</p> <pre><code>from pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    api_key: str\n    debug: bool = False\n    model_config = {\"env_file\": \".env\"}\n\ns = Settings()\n</code></pre>"},{"location":"reference/concepts/#typing","title":"Typing","text":"<pre><code>from typing import Protocol, TypeVar, Callable\n\n# Protocol \u2014 structural subtyping (duck typing with types)\nclass Drawable(Protocol):\n    def draw(self) -&gt; None: ...\n\ndef render(item: Drawable) -&gt; None:\n    item.draw()\n\n# TypeVar \u2014 generic functions\nT = TypeVar(\"T\")\n\ndef first(items: list[T]) -&gt; T:\n    return items[0]\n\n# Callable \u2014 type-hint functions\ndef apply(f: Callable[[int], int], x: int) -&gt; int:\n    return f(x)\n</code></pre>"},{"location":"reference/concepts/#context-managers","title":"Context Managers","text":"<pre><code># Using with (file)\nwith open(\"data.txt\", \"r\", encoding=\"utf-8\") as f:\n    content = f.read()\n\n# Custom context manager with contextlib\nfrom contextlib import contextmanager\n\n@contextmanager\ndef managed_resource():\n    resource = acquire()\n    try:\n        yield resource\n    finally:\n        release(resource)\n</code></pre>"},{"location":"reference/concepts/#comprehensions","title":"Comprehensions","text":"<pre><code># List comprehension\nsquares = [x**2 for x in range(10) if x % 2 == 0]\n\n# Dict comprehension\nword_lengths = {word: len(word) for word in [\"hello\", \"world\"]}\n\n# Set comprehension\nunique_lengths = {len(word) for word in [\"hello\", \"world\", \"hi\"]}\n\n# Nested\nmatrix = [[i * j for j in range(3)] for i in range(3)]\n</code></pre>"},{"location":"reference/concepts/#functools-quick-reference","title":"<code>functools</code> Quick Reference","text":"Function Use <code>functools.wraps(func)</code> Copy metadata to wrapper <code>functools.reduce(f, iterable)</code> Fold left with binary function <code>functools.partial(f, *args)</code> Pre-fill arguments <code>functools.lru_cache(maxsize=128)</code> Memoise return values <code>functools.cache</code> Unbounded memoisation (Python 3.9+)"},{"location":"reference/projects/","title":"Project Catalogue","text":"<p>Complete reference for every project in the repository.</p>"},{"location":"reference/projects/#all-projects","title":"All Projects","text":"Folder Theme Primary Skills Key Files <code>FuncMage</code> Functional / Decorators Lambdas, HOF, closures, decorators, <code>functools</code> <code>decorator_mastery.py</code> <code>CosmicData</code> Data Validation Pydantic v2, <code>BaseModel</code>, validators, nested models <code>models.py</code>, <code>settings.py</code> <code>DataArchivist</code> File I/O File streams, context managers, binary mode <code>archivist.py</code> <code>DataDeck</code> Data Structures Dicts, sets, comprehensions <code>deck.py</code> <code>DataQuest</code> Generators <code>yield</code>, generator expressions, <code>itertools</code> <code>quest.py</code> <code>TheCodex</code> Typing <code>Protocol</code>, <code>TypeVar</code>, <code>Annotated</code>, generics <code>codex.py</code> <code>TheMatrix</code> Typing + HOF Protocols, type aliases, <code>Callable</code> typing <code>matrix.py</code> <code>CodeCultivation</code> OOP Classes, inheritance, <code>__dunder__</code> methods <code>cultivation.py</code> <code>CodeNexus</code> Modules / Imports <code>__init__.py</code>, relative imports, packages <code>nexus/</code> <code>GardenGuardian</code> OOP + Composition Composition over inheritance, <code>ABC</code> <code>guardian.py</code> <code>GrowingCode</code> OOP + Generators Class-based iterators, <code>__iter__</code>, <code>__next__</code> <code>growing.py</code>"},{"location":"reference/projects/#skill-coverage-matrix","title":"Skill Coverage Matrix","text":"Skill Projects <code>lambda</code> / <code>map</code> / <code>filter</code> FuncMage, DataQuest <code>functools.reduce</code> FuncMage Decorators + <code>functools.wraps</code> FuncMage Pydantic <code>BaseModel</code> CosmicData, GardenGuardian <code>field_validator</code> CosmicData <code>BaseSettings</code> / <code>.env</code> CosmicData <code>yield</code> / generators DataQuest, GrowingCode <code>Protocol</code> / structural subtyping TheCodex, TheMatrix <code>TypeVar</code> / generics TheCodex <code>contextlib</code> / <code>with</code> statements DataArchivist <code>__iter__</code> / <code>__next__</code> GrowingCode <code>ABC</code> / abstract methods GardenGuardian"},{"location":"reference/style-rules/","title":"42 Norminette &amp; Style Rules","text":"<p>Python exercises at 42 are graded against PEP 8 via <code>pycodestyle</code>. This page documents every rule that comes up repeatedly in submissions.</p>"},{"location":"reference/style-rules/#run-the-linter","title":"Run the Linter","text":"<pre><code>pip install pycodestyle\npycodestyle --max-line-length=79 your_file.py\n</code></pre> <p>Zero output = zero errors. Any output = fix before submitting.</p>"},{"location":"reference/style-rules/#rules-that-come-up-most","title":"Rules That Come Up Most","text":""},{"location":"reference/style-rules/#line-length-e501","title":"Line Length \u2014 E501","text":"<p>Max 79 characters per line.</p> <pre><code># \u274c Too long\nresult = some_function(argument_one, argument_two, argument_three, argument_four)\n\n# \u2705 Break using implicit continuation inside parentheses\nresult = some_function(\n    argument_one,\n    argument_two,\n    argument_three,\n    argument_four,\n)\n</code></pre>"},{"location":"reference/style-rules/#blank-lines-around-definitions-e302-e303-e301","title":"Blank Lines Around Definitions \u2014 E302 / E303 / E301","text":"<pre><code># \u274c Missing two blank lines before top-level function\ndef foo(): pass\ndef bar(): pass\n\n# \u2705 Two blank lines between top-level definitions\ndef foo():\n    pass\n\n\ndef bar():\n    pass\n\n\n# \u2705 One blank line between methods inside a class\nclass MyClass:\n    def method_a(self):\n        pass\n\n    def method_b(self):\n        pass\n</code></pre>"},{"location":"reference/style-rules/#whitespace-around-operators-e225-e228","title":"Whitespace Around Operators \u2014 E225 / E228","text":"<pre><code># \u274c\nx=1\ny = x*2+1\n\n# \u2705\nx = 1\ny = x * 2 + 1\n\n# Exception: default argument values \u2014 NO spaces\ndef func(x=1, y=2): ...     # \u2705\ndef func(x = 1, y = 2): ... # \u274c\n</code></pre>"},{"location":"reference/style-rules/#trailing-whitespace-w291-w293","title":"Trailing Whitespace \u2014 W291 / W293","text":"<p>Configure your editor to strip trailing whitespace on save. In Neovim/Vim:</p> <pre><code>autocmd BufWritePre * %s/\\s\\+$//e\n</code></pre>"},{"location":"reference/style-rules/#comparison-to-none-e711","title":"Comparison to <code>None</code> \u2014 E711","text":"<pre><code># \u274c\nif x == None: ...\nif x != None: ...\n\n# \u2705\nif x is None: ...\nif x is not None: ...\n</code></pre>"},{"location":"reference/style-rules/#comparison-to-truefalse-e712","title":"Comparison to <code>True</code>/<code>False</code> \u2014 E712","text":"<pre><code># \u274c\nif flag == True: ...\nif flag == False: ...\n\n# \u2705\nif flag: ...\nif not flag: ...\n</code></pre>"},{"location":"reference/style-rules/#import-order-e401-e402","title":"Import Order \u2014 E401 / E402","text":"<pre><code># \u2705 One import per line; stdlib before third-party before local\nimport os\nimport sys\n\nimport pydantic\n\nfrom .models import ContactLog\n</code></pre>"},{"location":"reference/style-rules/#what-42-graders-check-beyond-pycodestyle","title":"What 42 Graders Check Beyond <code>pycodestyle</code>","text":"Check Details Filename Must match the subject exactly (case-sensitive) Forbidden imports Read the subject \u2014 some ban <code>os</code>, <code>sys</code>, <code>re</code>, etc. <code>__pycache__</code> Must not be committed <code>.env</code> secrets Must not be committed Executable bit Some subjects require <code>chmod +x</code> on scripts Shebang <code>#!/usr/bin/env python3</code> required in executable scripts"},{"location":"reference/style-rules/#editor-setup-arch-linux-neovim","title":"Editor Setup (Arch Linux / Neovim)","text":"<p>Install <code>pycodestyle</code> as a linter source for <code>null-ls</code> / <code>nvim-lint</code>:</p> <pre><code>-- nvim-lint config\nrequire(\"lint\").linters_by_ft = {\n  python = { \"pycodestyle\" },\n}\n</code></pre> <p>Or with <code>conform.nvim</code> for auto-format on save using <code>autopep8</code>:</p> <pre><code>pip install autopep8\n</code></pre> <pre><code>require(\"conform\").setup({\n  formatters_by_ft = {\n    python = { \"autopep8\" },\n  },\n  format_on_save = { timeout_ms = 500 },\n})\n</code></pre>"},{"location":"tutorials/","title":"Tutorials","text":"<p>Tutorials are learning-oriented \u2014 they guide you through building something real, step by step. You don't need prior knowledge of the topic; by the end you'll have working code and a mental model of why it works.</p>"},{"location":"tutorials/#available-tutorials","title":"Available Tutorials","text":"<ul> <li> <p> Writing Your First Decorator</p> <p>Build a <code>spell_timer</code> decorator from scratch. Covers closures, <code>functools.wraps</code>, and chaining multiple decorators.</p> <p>~20 min \u00b7 Beginner</p> </li> <li> <p> Pydantic Data Modelling</p> <p>Model alien contact logs with Pydantic v2. Covers <code>BaseModel</code>, field validators, nested models, and <code>model_dump()</code>.</p> <p>~30 min \u00b7 Intermediate</p> </li> </ul>"},{"location":"tutorials/first-decorator/","title":"Tutorial: Writing Your First Decorator","text":"<p>Goal: By the end of this tutorial you'll have written a working <code>spell_timer</code> decorator \u2014 exactly the kind required in the FuncMage project \u2014 and you'll understand why each line exists.</p> <p>Time: ~20 minutes Prerequisites: Basic Python functions. No prior decorator knowledge needed.</p>"},{"location":"tutorials/first-decorator/#1-what-problem-does-a-decorator-solve","title":"1. What Problem Does a Decorator Solve?","text":"<p>Suppose you have several functions and you want to time all of them:</p> <pre><code>import time\n\ndef cast_fireball():\n    time.sleep(0.1)\n    return \"\ud83d\udd25 Fireball!\"\n\n# Na\u00efve timing:\nstart = time.perf_counter()\nresult = cast_fireball()\nend = time.perf_counter()\nprint(f\"cast_fireball took {end - start:.4f}s\")\n</code></pre> <p>That's fine for one function. For ten functions it becomes repetitive noise. A decorator lets you wrap that timing logic once and reuse it anywhere with a single <code>@</code> line.</p>"},{"location":"tutorials/first-decorator/#2-understanding-closures-first","title":"2. Understanding Closures First","text":"<p>A decorator is a function that returns a function. That inner function is called a closure \u2014 it \"closes over\" variables from the outer scope.</p> <pre><code>def make_greeter(greeting):  # (1)\n    def greet(name):          # (2)\n        return f\"{greeting}, {name}!\"  # (3)\n    return greet              # (4)\n\nhello = make_greeter(\"Hello\")\nprint(hello(\"Ahmad\"))  # \u2192 Hello, Ahmad!\n</code></pre> <ol> <li><code>make_greeter</code> is a factory \u2014 it creates customised functions.</li> <li><code>greet</code> is defined inside <code>make_greeter</code> \u2014 it's the closure.</li> <li><code>greeting</code> is captured from the outer scope even after <code>make_greeter</code> returns.</li> <li>We return the function object itself (no parentheses \u2014 don't call it!).</li> </ol>"},{"location":"tutorials/first-decorator/#3-your-first-decorator","title":"3. Your First Decorator","text":"<p>A decorator is just a closure factory where the outer argument is the function being decorated:</p> <pre><code>def spell_timer(func):          # (1)\n    def wrapper(*args, **kwargs):  # (2)\n        import time\n        start = time.perf_counter()\n        result = func(*args, **kwargs)  # (3)\n        elapsed = time.perf_counter() - start\n        print(f\"{func.__name__} took {elapsed:.4f}s\")\n        return result               # (4)\n    return wrapper                  # (5)\n</code></pre> <ol> <li><code>func</code> is the function we're wrapping \u2014 passed in automatically by <code>@spell_timer</code>.</li> <li><code>wrapper</code> accepts any positional and keyword arguments with <code>*args, **kwargs</code> so it works with any function signature.</li> <li>We call the original function and capture its return value.</li> <li>Always return the result \u2014 callers expect it.</li> <li>Return the <code>wrapper</code> closure, not the result of calling it.</li> </ol> <p>Apply it with the <code>@</code> syntax:</p> <pre><code>@spell_timer\ndef cast_fireball():\n    import time\n    time.sleep(0.1)\n    return \"\ud83d\udd25 Fireball!\"\n\ncast_fireball()\n# cast_fireball took 0.1003s\n</code></pre> <p>What <code>@spell_timer</code> actually does</p> <p><code>@spell_timer</code> above a <code>def</code> is exactly equivalent to writing: <pre><code>cast_fireball = spell_timer(cast_fireball)\n</code></pre> It replaces <code>cast_fireball</code> with the <code>wrapper</code> closure returned by <code>spell_timer</code>.</p>"},{"location":"tutorials/first-decorator/#4-preserve-the-functions-identity-with-functoolswraps","title":"4. Preserve the Function's Identity with <code>functools.wraps</code>","text":"<p>There's a problem with the decorator above:</p> <pre><code>print(cast_fireball.__name__)  # \u2192 'wrapper'  \u274c\nprint(cast_fireball.__doc__)   # \u2192 None        \u274c\n</code></pre> <p>The wrapper has replaced the original function's metadata. Fix this with <code>functools.wraps</code>:</p> <pre><code>import functools\nimport time\n\ndef spell_timer(func):\n    @functools.wraps(func)          # \u2190 copies __name__, __doc__, etc.\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        elapsed = time.perf_counter() - start\n        print(f\"{func.__name__} took {elapsed:.4f}s\")\n        return result\n    return wrapper\n</code></pre> <p>Now:</p> <pre><code>@spell_timer\ndef cast_fireball():\n    \"\"\"Cast a powerful fireball spell.\"\"\"\n    time.sleep(0.1)\n    return \"\ud83d\udd25 Fireball!\"\n\nprint(cast_fireball.__name__)  # \u2192 'cast_fireball' \u2705\nprint(cast_fireball.__doc__)   # \u2192 'Cast a powerful fireball spell.' \u2705\n</code></pre> <p>Always use <code>@functools.wraps</code></p> <p>Without it, introspection tools, test frameworks, and debuggers see <code>wrapper</code>  everywhere instead of your real function names. It's a one-liner with no downsides.</p>"},{"location":"tutorials/first-decorator/#5-chaining-decorators","title":"5. Chaining Decorators","text":"<p>You can stack multiple decorators \u2014 they apply bottom-up:</p> <pre><code>def log_spell(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        print(f\"\ud83d\udcdc Casting: {func.__name__}\")\n        return func(*args, **kwargs)\n    return wrapper\n\n@spell_timer   # applied second (outermost)\n@log_spell     # applied first (innermost)\ndef cast_fireball():\n    time.sleep(0.05)\n    return \"\ud83d\udd25\"\n</code></pre> <pre><code>cast_fireball()\n# \ud83d\udcdc Casting: cast_fireball\n# cast_fireball took 0.0503s\n</code></pre>"},{"location":"tutorials/first-decorator/#6-putting-it-all-together","title":"6. Putting It All Together","text":"<p>Here's the final <code>spell_timer</code> as it appears in FuncMage:</p> <pre><code>import functools\nimport time\n\ndef spell_timer(func):\n    \"\"\"Decorator that prints the execution time of the wrapped function.\"\"\"\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        elapsed = time.perf_counter() - start\n        print(f\"\u23f1  {func.__name__} executed in {elapsed:.6f} seconds\")\n        return result\n    return wrapper\n\n\n@spell_timer\ndef slow_incantation(n: int) -&gt; str:\n    \"\"\"Simulate a slow spell proportional to n.\"\"\"\n    time.sleep(n * 0.01)\n    return f\"Spell of magnitude {n} complete!\"\n\n\nif __name__ == \"__main__\":\n    print(slow_incantation(5))\n    print(slow_incantation(10))\n</code></pre> <p>Expected output:</p> <pre><code>\u23f1  slow_incantation executed in 0.050312 seconds\nSpell of magnitude 5 complete!\n\u23f1  slow_incantation executed in 0.100418 seconds\nSpell of magnitude 10 complete!\n</code></pre>"},{"location":"tutorials/first-decorator/#whats-next","title":"What's Next?","text":"<ul> <li>FuncMage project reference \u2014 see <code>power_validator</code>, <code>retry_spell</code>, and <code>MageGuild</code> built on these foundations.</li> <li>Pydantic Data Modelling tutorial \u2014 apply a similar \"wrapping\" mindset to data validation.</li> <li>Python Concepts Cheatsheet \u2014 quick-reference for all HOF and decorator patterns used across the curriculum.</li> </ul>"},{"location":"tutorials/pydantic-models/","title":"Tutorial: Pydantic Data Modelling","text":"<p>Goal: Model alien contact logs (as in CosmicData) using Pydantic v2. You'll learn <code>BaseModel</code>, field validators, nested models, environment config, and <code>model_dump()</code>.</p> <p>Time: ~30 minutes Prerequisites: Basic Python classes. No prior Pydantic knowledge needed.</p>"},{"location":"tutorials/pydantic-models/#1-why-pydantic","title":"1. Why Pydantic?","text":"<p>Python dicts and dataclasses give you no runtime validation:</p> <pre><code># This silently accepts garbage data\ncrew = {\"name\": \"Ahmad\", \"age\": \"not a number\", \"mission\": None}\n</code></pre> <p>Pydantic validates at instantiation time and gives you clear error messages:</p> <pre><code>from pydantic import BaseModel\n\nclass CrewMember(BaseModel):\n    name: str\n    age: int\n\nmember = CrewMember(name=\"Ahmad\", age=\"not a number\")\n# ValidationError: 1 validation error for CrewMember\n# age: Input should be a valid integer [...]\n</code></pre>"},{"location":"tutorials/pydantic-models/#2-your-first-model","title":"2. Your First Model","text":"<pre><code>from pydantic import BaseModel\nfrom datetime import datetime\n\nclass ContactLog(BaseModel):\n    signal_id: str\n    received_at: datetime\n    frequency_mhz: float\n    decoded_message: str | None = None  # optional field with default\n</code></pre> <p>Instantiate it:</p> <pre><code>log = ContactLog(\n    signal_id=\"SIG-001\",\n    received_at=\"2025-03-14T09:26:53\",  # Pydantic parses ISO strings automatically\n    frequency_mhz=1420.405,\n)\n\nprint(log.signal_id)        # SIG-001\nprint(log.received_at)      # 2025-03-14 09:26:53\nprint(log.decoded_message)  # None\n</code></pre> <p>Automatic coercion</p> <p>Pydantic v2 is strict by default for types but lenient for common conversions.  A string <code>\"2025-03-14T09:26:53\"</code> is automatically coerced to <code>datetime</code>.  A string <code>\"not a number\"</code> for a <code>float</code> field is not \u2014 that raises a <code>ValidationError</code>.</p>"},{"location":"tutorials/pydantic-models/#3-field-validators","title":"3. Field Validators","text":"<p>Use <code>@field_validator</code> to enforce domain rules:</p> <pre><code>from pydantic import BaseModel, field_validator\n\nclass ContactLog(BaseModel):\n    signal_id: str\n    frequency_mhz: float\n\n    @field_validator(\"signal_id\")\n    @classmethod\n    def signal_id_must_be_prefixed(cls, v: str) -&gt; str:\n        if not v.startswith(\"SIG-\"):\n            raise ValueError(\"signal_id must start with 'SIG-'\")\n        return v.upper()  # normalise to uppercase\n\n    @field_validator(\"frequency_mhz\")\n    @classmethod\n    def frequency_must_be_positive(cls, v: float) -&gt; float:\n        if v &lt;= 0:\n            raise ValueError(\"frequency_mhz must be positive\")\n        return v\n</code></pre> <p>Test the validation:</p> <pre><code>ContactLog(signal_id=\"INVALID\", frequency_mhz=1420.405)\n# ValidationError: signal_id must start with 'SIG-'\n\nContactLog(signal_id=\"SIG-001\", frequency_mhz=-5.0)\n# ValidationError: frequency_mhz must be positive\n\nlog = ContactLog(signal_id=\"sig-001\", frequency_mhz=1420.405)\nprint(log.signal_id)  # SIG-001  \u2190 normalised\n</code></pre>"},{"location":"tutorials/pydantic-models/#4-nested-models","title":"4. Nested Models","text":"<p>Models can contain other models:</p> <pre><code>from pydantic import BaseModel\nfrom datetime import datetime\n\nclass Location(BaseModel):\n    constellation: str\n    light_years_away: float\n\nclass ContactLog(BaseModel):\n    signal_id: str\n    received_at: datetime\n    frequency_mhz: float\n    origin: Location                  # nested model\n    decoded_message: str | None = None\n\nlog = ContactLog(\n    signal_id=\"SIG-042\",\n    received_at=\"2025-06-01T00:00:00\",\n    frequency_mhz=1420.405,\n    origin={\"constellation\": \"Orion\", \"light_years_away\": 1344.0},  # dict works too\n)\n\nprint(log.origin.constellation)  # Orion\n</code></pre>"},{"location":"tutorials/pydantic-models/#5-serialisation-with-model_dump","title":"5. Serialisation with <code>model_dump()</code>","text":"<p>Convert back to a plain dict for JSON serialisation, database writes, etc.:</p> <pre><code>data = log.model_dump()\n# {\n#   'signal_id': 'SIG-042',\n#   'received_at': datetime(2025, 6, 1, 0, 0),\n#   'frequency_mhz': 1420.405,\n#   'origin': {'constellation': 'Orion', 'light_years_away': 1344.0},\n#   'decoded_message': None\n# }\n\n# JSON-safe (datetime \u2192 ISO string):\nimport json\njson_str = log.model_dump_json()\n</code></pre>"},{"location":"tutorials/pydantic-models/#6-environment-config-with-basesettings","title":"6. Environment Config with <code>BaseSettings</code>","text":"<p>In CosmicData (and the <code>python-dotenv</code> exercises), config is loaded from <code>.env</code>:</p> <pre><code># .env\nDATABASE_URL=postgresql://localhost/cosmic\nMAX_SIGNALS=1000\nDEBUG=true\n</code></pre> <pre><code>from pydantic_settings import BaseSettings  # pip install pydantic-settings\n\nclass Settings(BaseSettings):\n    database_url: str\n    max_signals: int = 500\n    debug: bool = False\n\n    model_config = {\"env_file\": \".env\", \"env_file_encoding\": \"utf-8\"}\n\nsettings = Settings()\nprint(settings.database_url)  # postgresql://localhost/cosmic\nprint(settings.debug)         # True  \u2190 coerced from string \"true\"\n</code></pre> <p>Pydantic Settings is separate</p> <p>In Pydantic v2, <code>BaseSettings</code> moved to the <code>pydantic-settings</code> package.  Install it with <code>pip install pydantic-settings</code>, not just <code>pydantic</code>.</p>"},{"location":"tutorials/pydantic-models/#7-full-cosmicdata-example","title":"7. Full CosmicData Example","text":"<pre><code>from __future__ import annotations\n\nfrom datetime import datetime\nfrom pydantic import BaseModel, field_validator\n\n\nclass AlienCrewMember(BaseModel):\n    name: str\n    role: str\n    health: int = 100\n\n    @field_validator(\"health\")\n    @classmethod\n    def health_in_range(cls, v: int) -&gt; int:\n        if not (0 &lt;= v &lt;= 100):\n            raise ValueError(\"health must be between 0 and 100\")\n        return v\n\n\nclass SpaceStation(BaseModel):\n    station_id: str\n    crew: list[AlienCrewMember]\n    operational: bool = True\n\n    @property\n    def crew_count(self) -&gt; int:\n        return len(self.crew)\n\n\nstation = SpaceStation(\n    station_id=\"DEEP-SPACE-9\",\n    crew=[\n        {\"name\": \"Zorg\", \"role\": \"Commander\", \"health\": 95},\n        {\"name\": \"Glrix\", \"role\": \"Engineer\", \"health\": 78},\n    ],\n)\n\nprint(station.crew_count)       # 2\nprint(station.crew[0].name)     # Zorg\nprint(station.model_dump_json(indent=2))\n</code></pre>"},{"location":"tutorials/pydantic-models/#whats-next","title":"What's Next?","text":"<ul> <li>CosmicData project reference \u2014 the full model hierarchy used in the submission.</li> <li>How to Run Tests Locally \u2014 validate your models against the exercise specs.</li> <li>Python Concepts Cheatsheet \u2014 Pydantic v2 field types quick-reference.</li> </ul>"}]}